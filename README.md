Shows gathering all jar deps from the project and subprojects (all configurations) into build/deps

Just run:

    gradle copyAllDeps

Obviously dependencies in this project aren't used and were chosen at random as proof of concept ;-)